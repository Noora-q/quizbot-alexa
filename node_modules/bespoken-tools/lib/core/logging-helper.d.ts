/// <reference path="../../typings/index.d.ts" />
export declare class LoggingHelper {
    private static cli;
    private static verboseEnabled;
    private static logger;
    static setVerbose(enableVerbose: boolean): void;
    static debug(logger: string, message: string): void;
    static verbose(logger: string, message: string): void;
    static info(logger: string, message: string): void;
    static warn(logger: string, message: string): void;
    static error(logger: string, message: string): void;
    private static log(level, logger, message);
    static initialize(cli: boolean): void;
    private static formatter(options);
    private static cliFormatter(options);
}
