/// <reference path="../../typings/index.d.ts" />
import { Socket } from "net";
export declare class WebhookRequest {
    sourceSocket: Socket;
    rawContents: Buffer;
    method: string;
    uri: string;
    body: string;
    queryParameters: {
        [id: string]: string;
    };
    private headers;
    private requestID;
    constructor(sourceSocket?: Socket);
    static fromString(sourceSocket: Socket, payload: string, id?: number): WebhookRequest;
    append(data: Buffer): void;
    private appendBody(bodyPart);
    done(): boolean;
    contentLength(): number;
    isPing(): boolean;
    private parseHeaders(headersString);
    nodeID(): string;
    toTCP(): string;
    toString(): string;
    id(): number;
}
