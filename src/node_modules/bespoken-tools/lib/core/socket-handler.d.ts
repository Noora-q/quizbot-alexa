import { Socket } from "net";
export declare class SocketHandler {
    socket: Socket;
    private onMessage;
    buffer: string;
    onDataCallback: (data: Buffer) => void;
    onCloseCallback: () => void;
    private connected;
    static connect(host: string, port: number, onConnect: (error?: any) => void, onMessage: (message: string, messageID?: number) => void): SocketHandler;
    constructor(socket: Socket, onMessage: (message: string, sequenceNumber?: number) => void);
    private handleData(dataString);
    send(message: string, messageID?: number): void;
    remoteAddress(): string;
    remoteEndPoint(): string;
    disconnect(): void;
    isOpen(): boolean;
}
