export declare class LambdaServer {
    private file;
    private port;
    private verbose;
    private server;
    private dirty;
    private lambda;
    private watcher;
    private requests;
    private onDirty;
    constructor(file: string, port: number, verbose?: boolean);
    start(callback?: () => void): void;
    stop(onStop?: () => void): void;
    private invoke(request, body, response);
}
